#include <bits/stdc++.h>
#define INT_MAX 2147483647

using namespace std;

int main()
{
	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	int t;
	cin >> t;
	while(t--)
	{
		int n;
		cin >> n;
		int64_t num[n], a[n], b[n];

		for(int i = 0 ; i < n ; i ++)
			cin >> num[i];

		for(int i = 0 ; i < n ; i ++)
			cin >> a[i];

		for(int i = 0 ; i < n ; i ++)
			cin >> b[i];

		int64_t max = 0;
		int64_t tmp = 0;
		for(int i = 1 ; i < n ; i++)
		{
			
		}
	}
}